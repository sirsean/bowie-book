# Copy to `.env` for local tooling (never commit real keys).
# Used by `npm run generate:book-images`.

OPENAI_API_KEY=

# Optional default for the image harness when you omit `--book`
# OPENAI_BOOK_KEY=princess-bowie-fights-evil-santa
